from setuptools import setup
from setuptools import find_packages

setup(
    name = "amazon_laptop_scraper"
    version = "0.0.1"
    description = "Package that scrapes the information of the first 20 pages of laptops from searching in Amazon"
    url = "tbd"
    author = "Timothy Arnold"
    license = "tbd"
    packages = find_packages()
)